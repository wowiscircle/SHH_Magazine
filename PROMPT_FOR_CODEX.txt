You are continuing an existing SHH Magazine MVP. Do not rebuild or scaffold it from scratch.

Repository:
https://github.com/wowiscircle/SHH_Magazine

Before changing anything:
1. Clone or pull the latest `main` branch.
2. Read `AGENTS.md`, `README.md`, `CODEX.md`, `ACTION_ITEMS.md`, `docs/HANDOFF.md`, and all files under `docs/`.
3. Inspect `git status` and the recent commit history.
4. Run `npm ci`, `npm test`, `npm run lint`, and `npm run build`.
5. Report the current state and any mismatch between code, data, documentation, and deployment before implementing new work.

Current public preview:
https://shh-magazine.vercel.app

Current state:
- The MVP already contains Public pages, QR routing, a PDF.js Reader, event ingestion, and a protected Admin route in one Next.js app.
- Demo issues 2026-06, 2026-07, and 2026-08 have committed PDFs and covers.
- The public experience is mobile-first because visitors scan QR codes with phones. Treat 375/390px as the primary acceptance viewport.
- Preserve each PDF page's real portrait or landscape aspect ratio, lazy vertical rendering, page query routing, and zoom behavior.
- 2026-09 issue data, final placements, final QR matrix, approved doctor registration URL, durable analytics storage, and real Admin data are not complete.
- Some demo QR records target the missing 2026-09 issue. Do not print, distribute, or treat them as production QR codes.
- `/admin` currently returns 503 in the active deployment; verify non-empty Vercel Basic Auth variables and redeploy before Admin QA.
- The intended analytics path is the existing Google Apps Script Web App writing events to Google Sheet. Before integration, read `docs/QR_ANALYTICS_SETUP.md` and request the existing script, endpoint, Sheet schema, sample rows, access settings, authentication method, and final QR business mappings. Do not invent or replace the existing integration without reviewing it.
- Without a trusted exposure denominator, use QR entries/interactions or placement share; never call the metric scan rate.

Non-negotiable constraints:
1. Keep one app, one deployment, and one domain/origin.
2. Keep `/admin` protected inside the same app.
3. Public pages must not expose analytics, Admin, tracking-debug, or demo-dashboard UI.
4. Latest issue and practical-information routes remain metadata-driven.
5. Tracking failures must never block redirect or reading.
6. Registration redirects must use an allowlisted official HTTPS hospital host.
7. Do not fabricate issue titles, page numbers, placement IDs, QR destinations, analytics results, or credentials.
8. Except for the sanitized `.env.example`, do not commit `.env*`, `.vercel/`, secrets, or personal/patient data.
9. Use TDD for behavior changes and keep changes small.
10. Run `npm test`, `npm run lint`, and `npm run build` before handoff.
11. Do not deploy, change cloud access, or perform destructive actions unless the user explicitly requests it.
12. Communicate with the project owner in Traditional Chinese; keep code and code comments in English.

Start with the P0 items in `ACTION_ITEMS.md`. If the user has not supplied the real 2026-09 assets and registries, explain exactly what is missing and continue with the safest independent work that does not require fabricated data.
